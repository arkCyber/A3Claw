{
  "name": "typescript-weather-plugin",
  "version": "1.0.0",
  "description": "Weather forecast plugin written in TypeScript, compiled to WASM via Javy",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc && ../../scripts/build-plugin.sh . --release",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "keywords": ["openclaw", "wasm", "weather", "typescript"],
  "license": "MIT"
}
