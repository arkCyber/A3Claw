[package]
name = "skill-sort"
version = "0.1.0"
edition = "2021"
description = "OpenClaw+ sort skill plugin — sorting, searching, ranking (wasm32-wasip1)"

[lib]
crate-type = ["cdylib"]
name = "skill_sort"

[dependencies]
openclaw-plugin-sdk = { path = "../../plugin-sdk" }
serde_json = "1.0"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
