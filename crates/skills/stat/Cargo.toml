[package]
name = "skill-stat"
version = "0.1.0"
edition = "2021"
description = "OpenClaw+ stat skill plugin — 10 statistics skills (wasm32-wasip1)"

[lib]
crate-type = ["cdylib"]
name = "skill_stat"

[dependencies]
openclaw-plugin-sdk = { path = "../../plugin-sdk" }
serde_json = "1.0"

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
panic = "abort"
